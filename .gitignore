*.gem
*.rbc
.idea
*.iml
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
.byebug_history
docspring-generator/target
.gpg_passphrase

## Emacs files
*~

## OSX files
.DS_Store
Thumbs.db

## Ignore baked in swagger generator
swagger-codegen

# Not sure where these are coming from
bootsnap-compile-cache

# Don't check in any auto-generated clients.
# We push these up to their respective GitHub repos.
clients/*

# API Clients (OpenAPI Generator)
# Note: These jar files are included in the Docker build context,
# and embedded in the CI image. (Much cheaper than compiling
# everything from scratch on CI.)
openapi-generator-cli.jar
openapi-generator-cli-*.jar
docspring-generator.jar

# Local file to keep track of which versions we have already released to
# package managers and pushed to GitHub
.releases.json
